 //learning to map

 function doubleAll(numbers) {
 	var answer = numbers.map(x => x * 2);
 	return answer;
    }

module.exports = doubleAll

//arrow function = function function-name (x) {return x*2}.
//																				(x			=>    x*2)